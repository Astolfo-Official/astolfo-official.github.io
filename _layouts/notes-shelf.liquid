---
layout: page
---
{% assign collection = site[page.collection] %}
{% assign finished_notes = collection | where: 'status', 'Finished' %}
{% assign active_notes = collection | where: 'status', 'Building' %}

<div class="notes-shelf">
  <header class="page-hero notes-hero">
    <div class="notes-hero__copy">
      <h1>{{ page.title }}</h1>
      {{ content }}
    </div>

    <dl class="notes-stats" aria-label="Notes overview">
      <div>
        <dt>{{ collection | size }}</dt>
        <dd>manuscripts</dd>
      </div>
      <div>
        <dt>{{ finished_notes | size }}</dt>
        <dd>finished</dd>
      </div>
      <div>
        <dt>{{ active_notes | size }}</dt>
        <dd>active</dd>
      </div>
    </dl>
  </header>

  {% if collection and collection.size > 0 %}
    <section class="notes-section" aria-label="Core notes">
      <div class="notes-list">
        {% for item in collection reversed %}
          {% assign intro = item.content
            | split: 'Introduction:'
            | last
            | replace: '</li>', ''
            | replace: '</ul>', ''
            | markdownify
            | strip_html
            | strip_newlines
            | replace: '  ', ' '
            | replace: '  ', ' '
            | strip
          %}
          <article class="note-card">
            <a class="note-card-media" href="{{ item.url | relative_url }}" aria-label="{{ item.title }}">
              {% if item.cover %}
                <img alt="{{ item.title }} cover" src="{{ item.cover | relative_url }}">
              {% elsif item.olid %}
                <img alt="{{ item.title }} cover" src="https://covers.openlibrary.org/b/olid/{{ item.olid }}-L.jpg?default=false">
              {% elsif item.isbn %}
                <img alt="{{ item.title }} cover" src="https://covers.openlibrary.org/b/isbn/{{ item.isbn }}-L.jpg?default=false">
              {% else %}
                <span class="note-card-fallback">
                  <i class="fa-solid fa-book-open"></i>
                </span>
              {% endif %}
            </a>

            <div class="note-card-body">
              <div class="note-card-meta">
                {% if item.status %}
                  <span class="note-status note-status-{{ item.status | downcase }}">{{ item.status }}</span>
                {% endif %}
                {% if item.released %}
                  <span>{{ item.released }}</span>
                {% endif %}
                {% if item.categories %}
                  {% for category in item.categories %}
                    {% unless category == 'notes' %}
                      <span>{{ category }}</span>
                    {% endunless %}
                  {% endfor %}
                {% endif %}
              </div>

              <h3>
                <a href="{{ item.url | relative_url }}">{{ item.title }}</a>
              </h3>
            </div>

            {% if intro != '' %}
              <p class="note-card-summary">{{ intro }}</p>
            {% endif %}
          </article>
        {% endfor %}
      </div>
    </section>
  {% endif %}
</div>
